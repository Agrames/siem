# copy this to .env and fill it in. the real .env is gitignored, never commit it.

# postgres password (compose builds the DATABASE_URL from this)
POSTGRES_PASSWORD=change-me-in-prod

# alert webhook: a Slack or Discord incoming webhook URL. leave blank to disable alerts.
SIEM_WEBHOOK_URL=
